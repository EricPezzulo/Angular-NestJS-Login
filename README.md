# Angular NestJS Login Screen

To run the server:

```
cd server

pnpm install

pnpm start run
```

To start the client: (@Angular/cli must installed to use `ng`)

```
cd client

npm install

ng serve
```
